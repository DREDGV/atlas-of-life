{
  "name": "atlas-of-life",
  "version": "0.9.0-alpha.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start:sync": "node --no-warnings server/start.js",
    "test:sync": "node --no-warnings tests/inbox-sync-server.mjs && node --no-warnings tests/inbox-sync-e2e.mjs && node --no-warnings tests/inbox-sync-config.mjs",
    "build:android-web": "node tools/build-android-web.mjs",
    "test:android-prep": "node tests/android-prep.mjs",
    "android:add": "npm run build:android-web && npx cap add android && node tools/configure-android-project.mjs",
    "android:sync": "npm run build:android-web && npx cap sync android && node tools/configure-android-project.mjs",
    "android:open": "npm run android:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/keyboard": "8.0.2",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  }
}
