{
  "name": "atlas-of-life",
  "version": "0.9.0-alpha.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:android-web": "node tools/build-android-web.mjs",
    "test:android-prep": "node tests/android-prep.mjs",
    "android:add": "npm run build:android-web && npx cap add android",
    "android:sync": "npm run build:android-web && npx cap sync android",
    "android:open": "npm run android:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  }
}
